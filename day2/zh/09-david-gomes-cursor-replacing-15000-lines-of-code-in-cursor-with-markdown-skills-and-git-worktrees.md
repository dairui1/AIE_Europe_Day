# David Gomes (Cursor)：使用 Markdown 技能和 Git Worktrees 替换 Cursor 中的 15,000 行代码

- Source: _zdroS0Hc74.segmented.md
- Time: `02:26:10` → `02:46:17`

**Time:** `02:26:10` → `02:46:17`

>> 各位好。你们都还好吗？非常感谢今天的到来。嗯，我将会讨论 markdown 基本上如何成为新的代码。嗯，正如 Tejas 已经预告的那样，我们最近用 markdown 技能替换了 Cursor 应用中的大量代码。在今天的演讲中，我将分享从一个拥有大量代码、许多依赖关系、复杂性和测试的完整功能特性转变为一个更轻量级、简化版本、但仅使用单个技能的相同功能特性的旅程。嗯，但在我开始之前，我必须给各位一个关于 git worktrees 以及它们如何在 Cursor 中工作的简短回顾。现在，如果你还没有听说过 Git 中的 worktrees，它们实际上就像嗯独立的检出，我为白屏道歉，但它们实际上就像你的仓库的独立检出，允许你并行工作。所以，不同的代理可以同时处理相同的任务或在同一时间处理不同的任务，而不会相互干扰。如果你在 Cursor 中从未使用过此功能，其工作方式是你可以在单个 worktree 上启动一个代理。嗯，你将看到例如在两个不同的 worktrees 中相同的文件，你可以看到它们看起来不同，因为代理在 worktree 上做一些工作，但不在你的主检出上。代理运行的任何命令、linting 或任何操作都将被隔离并限制在该 git worktree 中。嗯，有了这个功能你也可以嗯在屏幕上同时并行工作。你可以有类似这些代理网格为你工作。嗯，如果你说"嘿，打开一个 PR"，代理将从该 worktree 打开一个拉取请求，其中包含它在该 worktree 内产生的更改。这个功能最酷的事情之一是它允许你同时给不同的模型分配相同的任务，然后比较不同的模型对相同提示的作用。所以如果你还没有听说过这个，我们称之为 best event，它实际上是一种方式让你在不同的差异上竞争，让不同的模型在相同的任务上竞争。然后你甚至可以预览更改，如果它是一个前端嗯项目你正在处理。嗯，你可以嗯比较所有不同的视觉实现，然后选择你喜欢的。现在，如果你从未听说过这些，关于我今天要讲的所有内容，嗯我只是要说这一切都是在去年大约十月左右推出的，与 Cursor 2.0 一起。嗯，当我们最初推出它时，它附带了大量复杂性。嗯，我们必须编写所有用于创建 worktrees、管理这些 worktrees、将它们作为上下文输入代理的代码。我们还必须确保代理被限制和隔离，他们无法逃脱他们正在工作的 worktree。呃，我们还有一些所谓的设置脚本，用户可以配置并运行呃和和让 cursor 在任何时候代理开始在给定的单词上运行时运行它们。我们还有判定。所以，我之前没有向你展示这个，但呃有一个小的赞成图标在其中一个模型上。那只是一个我们运行的判定，嗯告诉你哪个实现看起来最好基于嗯不同的标准。呃然后我们还必须对工具进行一些更改呃并引入一些系统提醒来帮助代理在这些 worktrees 中保持专注。最后，还有一些清理复杂性，因为人们喜欢启动数百个这些 worktrees，然后他们的磁盘大小爆炸了，我们必须帮助他们通过清理呃呃留下的 worktrees 来帮助。现在，在我们的新实现中，我将讨论的那个，我们能够摆脱大部分这些东西。实际上，我最近打开了一个 PR，从 Cursor 中删除了这整个功能，这是一个大规模的嗯嗯代码删除。我认为这大约是 15,000 行代码被删除。该功能的新实现几乎与之前的实现一样好。嗯，在我们维护它的术语中它要轻得多。嗯，它甚至与我今天将讨论的之前实现相比有一些好处。那么，我们如何能够用一个技能替换一整个功能呢？我们决定有两个基本元素，我们可以使用它们来有效地允许 Cursor 用户通过简单地利用两个基本元素来使用 worktrees。一个是 Asian 技能，另一个是子代理。所以，这两个都是现有的 Cursor 功能。你可以在我们的文档中了解更多关于它们的信息。呃我们有一个关于技能的页面，我们有一个关于子代理的页面。我们意识到，如果我们一起把这两件事拿在一起，我们基本上可以用 markdown 重新实现 Cursor worktrees 功能和 Cursor best event 功能。这是一个关于它如何工作的小视频。所以我现在可以作为用户说 slashwork tree，然后我给它一些任务。我会说修复网站页脚中的一个拼写错误，这个代理将在一个隔离的 worktree 中运行并在那里完成工作。所以技能的编写方式实际上非常简单。我可以向你展示大部分。呃它不适合屏幕，但它基本上是一组指令，告诉模型嗯如何创建 worktrees 和嗯运行用户可能已配置的设置脚本，然后留在该检出上，对吧？我们想确保当代理在 worktree 上运行时，它停留在该检出中。呃best event 技能非常相似。它实际上更小。整个技能适合这里的屏幕，带有小的呃字体。嗯，我们在这里做的是我们指示父代理去为每个模型创建子代理，然后为每个呃启动一个 worktree，所以让每个子代理创建自己的 worktree 并在该 worktree 内工作。嗯，然后我们也告诉它等待所有子代理。当他们完成时，请提供一些评论。请让用户知道嗯不同的子代理的不同实现看起来像什么。也许你可以给他们评分。也许你可以对他们进行一些呃批评，也许你可以帮助用户选择哪一个是最好的。嗯和和请在一些很好的表格格式或类似的东西中将其提供给用户。但再次，它只有大约 40 行代码，这全是 markdown。就像它甚至不是代码。之前的版本可能是 4,000 行代码。我们在这个技能中必须考虑的一些事项是技能必须跨平台兼容，比如我们有 Windows 特定的说明，我们也有 Linux 和 Mac OS 说明。我们还指示父模型为用户可能已配置的每个 worktree 运行设置脚本。然后这是最难的部分。我们将在今天的演讲中花费一些时间讨论这个。我们必须指示模型停留在该 worktree 上，对吧？我们必须真的说，嘿，永远不要在此之外工作，也不要永远嗯逃脱，对吧？嗯和我们通过一些激进的提示有效地做到这一点。所以新命令是 slashword tree，然后 slashbass event 来做基本上嗯嗯启动代理在隔离的 worktrees 中和启动多个代理在相同的任务上。然后我们也有应用 word tree 和删除 word tree 来从侧面 worktree 将更改引入你的主检出。删除树就是做呃你期望的。呃一个小注意是这些实际上不是 Cursor 中的技能。他们实际上是命令。但这些命令在 Cursor 中的工作方式极其相似于技能的工作方式，即只有在用户选择加载它们时，提示才会被加载到上下文中。呃我们之所以做成命令而不是技能的唯一原因是为了能够在我们的服务器在我们的后端控制他们的提示。这意味着我可以迭代这些提示呃没有你必须更新你的 cursor 版本。呃如果我对这些提示进行了一些改进，下一次你使用它们，你将要有你将要得到最新版本的提示，但有效地他们的工作就像技能。呃这是最好事件的演示呃技能或命令，我给 Kimi、Grok、Composer、GPT 和 Opus 相同的任务。你将看到的是父代理通过在我指定的五个不同模型上启动五个子代理开始。每一个都有自己的 worktree。每一个都有自己的上下文。然后 Opus 如预期所需花费一点时间。然后在最后父模型根据说明将做出所有不同子代理之间的那个比较。它会说嗯这两个模型基本上做了相同的事情。这个做了其他的都没有做的事情。你甚至可以与父代理交谈，你可以说，"哦，我喜欢 Opus 做的这个部分，我喜欢 GPT 做的这个部分。你能能把他们加在一起吗？"和和父代理会为你做到这一点。嗯，所以让我们谈论新实现的一些优点，然后我将讨论一些的一些缺点，我们在这个重构中失去的东西。所以重新实现这个整个功能作为一个技能的主要优点是我有很少的代码来维护。呃自私地嗯我将花费很少的时间维护这个功能。这是一个嗯高级功能，对吧？我们不是在讨论一个被 90% 的游标用户使用的功能。远不止。Worktrees 是有点高级的东西。嗯，所以只有喜欢麻痹和有这些代理网格的 Cursor 高级用户正在使用 worktree。所以这不是我们想花很多时间维护的功能类型。另一个优点是我们的用户现在可以在聊天过程中半途切换到 worktree。之前是不可能的。嗯我们不想用所有这些像下拉菜单和设置一样污染提示 UI 太多。现在这只是一个斜杠命令，用户更容易中途切换到 worktree。他们可以开始讨论一些东西，然后如果他们决定他们想在网站上工作，他们可以用 slashword tree 做到这一点。另一个大优点是之前的实现如果你同时在多个仓库上工作时不起作用。所以很常见有一个多仓库设置，其中也许你的前端和后端是分离的仓库。过去你无法在这种设置中做 worktrees。它只是被禁用了。有了新的 /wart tree 命令一切工作正常。代理将确保在每个仓库上创建一个 worktree，然后如果你打开一个 PR 它将打开两个 PRs，一个用于每个仓库。它运行得相当好。新技能实现的另一个优点是在最后判定体验知道模型在最佳事件中做了什么是远优越的。父现在有很多更多的关于每个子代理做了什么的上下文。用户甚至可以要求代理缝合在一起一个小不同的部分和位来自不同的实现，这在之前的实现中是不可能的。你必须选择一个子代理或一个模型并坚持那个。现在，让我们讨论一些缺点。如果你很好奇，嗯，我们有一个论坛链接在这里，我们实际上在新家中得到了一些混合的反馈。比如，一些人真的习惯了该功能以前工作的旧方式。嗯，如果你很好奇，你可以去看到不是每个人都对这个改变满意，至少现在，但我们正在跟踪。问题是什么？第一，让代理保持专注是非常困难的。有了我们之前的方法，嗯，代理必须保持专注。就像我们没有让模型曾经接触任何文件在其 worktree 之外。它在物理上不可能为它做这个。现在我们相信这个模型。所以可以说这有点基于感觉，因为我们基本上在说，嘿，在这个目录上运行和和然后就像你知道的那样，敲木头，请不要忘记这个。特别是在长时间的会议中，模型很可能会忘记它应该在哪里运行。有时这些模型，特别是最坏的模型，会有点幻觉或他们会有点发疯并开始做他们不应该做的事情。但我们呃我们在这个上工作。呃，另一个缺点是它感觉更慢，因为你呃看到代理创建 worktree 并且你在你的聊天中看到这个。这实际上并不更慢，但它确实感觉像代理有点呃浪费时间做一些应该提前为它做的事情。呃，我们也在寻看一些改进这里。然后最后，现在找到该功能要困难得多，对吧？就像在之前打开 cursor 时，你有这个下拉菜单，会显示给你是否想在本地运行此任务或者你想在云中运行它或者你想在 worktree 中运行它。现在整个下拉菜单都消失了。所以如果你想使用 worktrees，你必须知道该功能存在，所以你实际上可以输入 slashwordree，所以可发现性有点更糟，但如我之前提到的，这是一个高级高级用户功能嗯，我们个人很好，我们呃好吧，在一般来说，不那么容易被发现。所以，我们如何能够使这个技能更好呢？嗯，如我提到的，现在最大的问题是代理并不总是保持专注。呃，有两种方式我们将改进这个。一种是用 evals，然后使用那些 eval 来改进提示，然后另一个是通过 RL 和训练。所以，在 Cursor，我们训练我们自己的模型称为 Composer。对于 Composer 2，我们呃这个模型的最新版本，我们没有任何带有这些提示的 RL 任务。我们呃我们没有任何任务在所有许多许多数千个任务中，我们呃使用用于 RL 的实际上在这种类型的环境中运行。所以我们正在工作添加一堆这些任务到我们的 RL 管道中，这样当我们启动 Composer 3 或 4 或 5 的时候至少我们自己的模型会在这个方面好得多。显然我们无法改进其他公司开发的模型，但我们一直在与所有其他实验室和模型提供商分享关于这种事情的反馈。对于 evals，呃我一直在为这个功能工作 evals，它实际上我呃不是我的第一次但一次我呃相当呃早期在我呃写 evals 呃旅程，我实际上非常惊讶，如果你使用类似 Braintrust 和呼喊对 Braintrust 的东西。他们一直超级有帮助。呃写这些类型的电子邮件呃实际上是非常非常容易的。你几乎不必知道关于 evals 的任何东西，你可以只是提示代理。它会为你做一切。嗯，有效地我正在做的是我启动 Cursor CLI。它是无头的，所以对于 evals 来说很棒。嗯，然后我有两个打分员。一个检查看模型是否在其 worktree 中按预期做了任何工作。然后另一个哪个是那个的反面，哪个做了模型在主检出中做了任何工作，其中它不应该做任何工作。呃，到目前为止我得到的 evals 是相当简单的。所以我实际上还没有能够呃模拟极其长的会话，这是当模型开始表现更差的时候。但即便如此，我已经理解不是所有模型都同样擅长这个。所以例如 Haiku，这是一个更小的、不太聪明的模型，会很经常偏离并开始在主检出中工作。但我一直在测试的其他模型，如 Composer 和 Grok 嗯做得好得多。所以我仍然必须大大改进这些 evals 使他们更复杂。但希望是一旦我可以开始找到这里的模式，我可以实际去改进提示。然后另一件我们可以做的事是对模型有更好的系统提醒呃指示他们保持专注并且不要偏离他们应该在其中工作的 worktree。好的。那么接下来呢？嗯，首先我们实际上要采取呃一个小步骤回到这里，我们实际上要有一个许多更完整和本地 worktrees 实现在新的 Cursor 代理窗口中。如果你呃一直在关注，我们最近宣布了 Cursor 3.0。3.0 的一部分知道是一个更代理化的编码界面，其中你仍然可以编辑代码，你仍然可以看到代码，但用户界面和用户体验更多是围绕代理和聊天界面优化的。我们相信这种类型的界面是正确的地方，用于一个合适的 worktrees 实现。更可能进行一堆本地平行化的类型的人通常也是更可能使用这种类型的用户界面的同一类型的人。所以我们在那里采取一个小步骤回来，并构建一个合适的 worktrees 呃实现，在新用户界面中更多的本地，不是那么多的代理化。另外，我们改进了技能呃如我提到的通过这项继续工作在 evals 上，然后 RL 和其他培训工作。然后最后，我们实际上在寻看不是 git worktrees 的其他平行化原始物。所以如果你使用过 git worktrees，你可能知道呃他们可以有点慢创建。嗯和也要呃他们也用尽了很多磁盘空间在你的电脑上。嗯和然后最后呃他们只在 git 仓库中工作。所以如果你使用除了 git 之外的东西，在 Cursor 中真的没有本地平行化原始物。呃，在不久的将来，我们希望呃分享更多关于这个，但我们在寻看一些其他解决方案，用于不涉及 git 并且不涉及 git worktrees 的本地平行化。呃，所以是的，敬请期待那个。呃，非常感谢大家今天来参加演讲。呃，我肯定很多人有问题，我将在整天都在。呃，随时来抓我，呃，嗯，我很高兴与任何人聊天。谢谢。David Gomez，很好地做出了奇妙的。是的，我们将有一些时间改变下一个扬声器将上来并开始设置。如果你想去抓呃另一个分组讲话在其他房间，这里是你的日程。呃这是编码代理轨道。欢迎。我们现在要讨论呃关于一个馅饼。谁喜欢吃馅饼？是的，没人。好的。谁喜欢啊我不知道用 pi 编码没人你醒着吗？呃是的一个人嘿你叫什么名字伙计呃Alex呃Alex为了亚历克斯给了它每个人，这是呃规则这个房间的规则是像亚历克斯一样，你知道吗，所以他将要与我们讨论如何在你的产品中嵌入开放爪编码代理，谁在这里使用开放爪编码代理不像四个好的嗯在这个演讲之后这个数字将上升，因为他将向我们展示如何在你的产品中使用它。这真的是一个不可思议的演讲。我得到了与 Matias 沟通，就在呃之前，我非常兴奋。所以，请你最热烈的掌声给了 Matias。呃呃。好的。呃谢谢你非常感谢我。呃真的很荣幸讲话
