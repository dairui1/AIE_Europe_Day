# Nick Taylor (Pomerium): 使用 Pomerium 身份感知代理加固 OpenClaw，以及从 Discord 实时编写 MCP 服务器

- Source: O_IMsEg91g8.segmented.md
- Time: `03:57:05` → `04:14:35`

**Time:** `03:57:05` → `04:14:35`

>> 好的。嗯，就像 Phil 说的，我在 Pomerium 工作，他也不是第一个念不准这个名字的人。所以，我其实说服了市场团队制作了博美犬（Pomeranian）贴纸。如果有人想要博美犬贴纸，我带了一堆。呃，简单介绍一下我自己。我在 Pomerium 担任开发者布道师。正如 Phil 所说，我来自加拿大，成长于蒙特利尔。所以，如果有人喜欢 poutine 和百吉饼，欢迎散场后来找我聊。另外，我也是 GitHub Star、Microsoft MVP 和 AWS Community Builder。这些地方基本上都能找到我，网络ID 是 Nikit T。我很高兴看到这里有一个规模相当可观的 OpenClaw 本地实例，这让我很开心。那位看起来就是负责运营的人，很酷。嗯，我起了个挺有趣的标题，叫做"亮出爪子"。我们要聊一聊我在今年二月向 open claw 项目贡献的一个特性，它关于的是如何加固控制平面的访问安全。我假设在座各位都在运行 OpenClaw，或者至少对 OpenClaw 很感兴趣。有没有人在运行一种叫做"可信代理关闭模式（trusted proxy off mode）"的模式？可能没有，不过也许有关闭令牌的人？好的。不管怎样，在我工作的 Pomerium，我的日常工作之一就是让事情更加安全。我成功地保护了 Open Club，但这意味着我仍然需要为 websocket 连接添加一个令牌，而且每次都要配对我的设备等等。而如果使用可信代理，尤其是我工作的这个开源代理，也就是所谓的身份感知代理（identity-aware proxy），就不需要这些了。如果有人用过 GCP，里面有一个叫做 identity-aware proxy 的东西，它起源于 Google。本质上，它由一个身份提供商、一个策略引擎和一个反向代理组成。这不是通常意义上的"致命三角"，但它是一种保护内部应用程序的相当可靠的安全方案。于是我就想，我真的很烦每次都要添加这个令牌、每次都要进行设备配对。我理解为什么需要这些，但还是提了一个 issue，然后至少有一位使用 Caddy 的朋友响应说："嘿，这听起来是个好主意。"接着 Peter Stipe 也说："好，我们来做这件事。"他列出了这个特性需要满足的标准，于是我就着手去做了。对，正如之前说的，在可信代理关闭模式之前，即使你已经用代理保护了访问，你仍然需要在 UI 中粘贴 OAuth 令牌来建立 websocket 连接，而且这个令牌还会出现在 query string 里，显然这种方式只适合纯本地模式。而且仍然需要配对设备——我不知道别人会不会觉得配对设备很烦，但对我来说，在手机上刚设置好，还得去另一个地方再操作一次，真的挺麻烦的。所以，即使你用代理保护了访问，还是要做这些额外的事情。这个特性最终被合并进来了，我感觉非常好，能得到 Peter 的赞赏也很开心。这是我第一次向这个项目做贡献，所以非常有意义。那么在配置文件里具体是什么样子呢？我这里只展示配置的一小部分，但你可以看到，你的 gateway 里不再需要令牌了，就像我之前说的。模式的名字也变了，现在叫做 trusted proxy。然后需要添加一些新的属性，有一个 `trusted proxies` 字段，它本质上就是那个把守控制平面访问的代理，也就是 gateway 的 IP 地址，可以是一个或多个。除此之外，你还需要一个 `trusted proxy` 配置段，其中有一个 `user header` 字段，在我的例子里是 JWT。然后还有一个 `required header` 配置段，以及一些可选项，具体取决于你想做什么。比如有 `allowed users` 这样的选项，但我的情况下不需要，因为 identity-aware proxy 的工作方式是由策略来决定访问权限的。总体来说，这就是主要的改动。你可以在入门引导流程中进行配置，也可以直接通过 TUI 返回修改配置。这样就意味着 websocket 连接不再需要令牌，也不再需要配对设备了。所以这不仅是一个安全提升，对我来说也是一个用户体验的胜利，因为我真的觉得这两个步骤很烦。很好。另外，我也想感谢在我贡献之后的几位社区贡献者。这个特性合并后出现了一个 bug，Anthony 报告了它，然后 Sid 修复了它。这确实是我当时遗漏的问题，因为我基本上是在本地环境上测试的，而我已经配对过设备，所以没有遇到 Anthony 提到的那个问题。好在这只是一个小修复，Sid 解决了它，但这就说明，当你遗漏了某些东西，社区里的人会站出来。开源万岁。我还想提另一件事，虽然和这个特性本身关系不大，但当我提交这个 issue 的时候，issue 编号是 1560。我当时有一个 PR，编号大概在 1700 多。然后我去度假了，想着回来再处理。原来那个 PR 因为太久不活跃被关闭了。就在两周之后，issue 编号从 1500 多涨到了差不多 16000。这就说明这个项目有多受欢迎。但这也意味着我在合并之前需要进行相当多的 rebase。不管怎样，我不知道还有没有其他人向这个项目贡献过，但这个项目随时都有很多事情在发生，需要不断 rebase 来保持你的代码最新。好的，来说说我自己的 OpenClaw 吧。这是 Mclaw，它现在正坐在我蒙特利尔的书桌上，那里现在还有一些雪。我在 Discord 上使用它。我一开始是在 Telegram 上用的，但 Telegram 的频道并不加密，所有的内容都是明文的。我在一家安全公司工作，我们 CEO 说："不，别用那个。"所以，我主要用 Discord，我觉得很方便。我也有 WhatsApp，但我更习惯用 Discord。还有一件事我想提一下，当初我做那个贡献时，我实际上是用 OpenClaw 来完成这个贡献的，这挺有趣的。但我也犯了一个错误，我用了 GitHub CLI，并且给了它完整权限，所以在我还没审查完之前，它就直接提了一个 PR。我有点慌，不过还是把它改回了草稿模式。除此之外，在可信代理模式合并之后，我就开始做一些有趣的事情，在手机上随手构建各种东西变得越来越有趣。于是我构建了一个叫做 Clawspace 的东西。这不是说你一定要用它，它只是这个"个人软件时代"里我自己玩儿出来的东西，我觉得很好用，而且能在手机上通过 Discord 搭建出这个，我觉得很酷。对我来说，它很实用，因为我不需要 SSH 进去就能查看或编辑工作区里的文件。这就是我在做的一个小项目，你也可以用它来编辑文件等等。好的，接下来我们要做一个演示，这是实时编码，yolo。嗯，明天有一个 MCP 专题，我最近在 MCP 方面做了很多工作。我们要做的是构建一个 MCP——不是一个完整的成品，但如果你看过 AI Engineer 网站，右边有一个 LM 文本框，还有一个 MCP 服务器和一些其他东西。我来在这里加上这个，然后去创建一个应用。我稍后会解释一些细节。好，然后是 OAS。好的，这会在 ChatGPT 里创建一个应用。这本质上是一个带有 UI 的 MCP 服务器，明天他们会讲这个，我有一个用来做这件事的模板，所以并不是从头开始构建，我们只是在这里注册一下这个 MCP，然后开始用 OpenClaw 来构建。而 Agentic 模式的特点是，你永远不知道它什么时候结束，现在它在完成 OAuth 认证。好，连接成功了。可以看到这里有两个工具，一个是 echo 工具，另一个是 search speakers 工具。如果没有人用过 MCP 应用，在 ChatGPT 里，你基本上是这样操作的。我来输入 "echo hello"，它会调用这个工具，由于它关联了 UI，你会在这里看到一些 UI 界面。这用的就是 MCP 规范里的标准内容。现在，你可以做一些事情，比如把字体改大之类的，但我真正想展示的是，当我用 OpenClaw 来构建这个时，我可以在 Discord 里直接说，比如："在 echo 小部件里把回显消息改成 AIE EU。"这会需要一点时间，但底层全是 Web 技术。如果有人是前端开发者，它底层用的是 Vite 和 React，有 React 热更新和 Vite 模块热重载。Mloud 在努力工作，你可以看到我在 ChatGPT 里，从我的工作区实时编辑这个 MCP。解释一下这是怎么做到的：我们有可信代理关闭模式，在这个例子里我恰好使用的是 Pomerium，用它来保护工作区里的其他东西。我有一个公开 URL，我用它来保护这个 MCP，这就是我能在 ChatGPT 里使用它的原因。我可以继续在这里修改它。我不知道别人是怎么使用或构建 OpenClaw 的，但这是我的方式，我觉得它特别适合 Web 开发。接下来我来说更新搜索演讲者的功能。开一个新对话，再叫上 AIE，搜索演讲者。这里的 UI 会非常简陋，因为里面没什么内容。所以我来叫 Mlaw 来处理这件事。基本上，如果你去 AIE 网站的右上角，有一个 speaker.json，里面有这次大会的所有演讲者。我们将用它作为用户数据源，然后让它做出跟 echo 小部件里类似的 UI。这可能需要一点时间，因为 claw 可能正被蒙特利尔的雪覆盖着。好，等这个完成之后，我们就能筛选用户，看看谁在大会上发言了。我先喝口水，等 Mlaw 跑完。再说一遍，Agentic 任务什么时候结束是不确定的，可以说是"确定性的不确定"。好，马上就好，然后你就会看到它更新了。再重申一遍这个流程：我在自己的 OpenClaw 里操作工作区文件，在 Discord 里用语音或文字指挥它，这是一个公开可访问的站点，我可以在自己的 OpenClaw 里即时构建它。我喜欢这个工作流。我也不太清楚别人是怎么工作的，我也用 Claude 和 Codex 等工具，但可以看到 Mclaw 完成了任务，我现在可以开始筛选了。我们可以在这里向下钻取，找到一位演讲者，然后查看更多信息。然后我还可以说，我们再加一个功能。来叫 Mlaw 继续工作吧，我们要在这里添加一个"更多"按钮。MCP 应用里有一个 `send message` 函数，当你点击这个将要生成的"更多"按钮时，它实际上会向 LLM 发起一次调用，然后你会得到响应。好，等一下。很好，"更多"按钮添加进来了。我做了很多年的 Web 开发，但每次看到东西自动刷新出来，我还是觉得很神奇。我来点击这里，你会看到它正在思考，它实际上向 ChatGPT 添加了一个新的 prompt，然后它会对为什么你应该关注 Aleandro 的演讲以及更多相关信息做一个摘要。我真的觉得这个工作流太酷了。这只有在使用某种代理的情况下才能实现。你可以用带 OAuth 的 Caddy 来做这件事，也可以用 Nginx，虽然 Nginx 在 Kubernetes 里 ingress controller 已经处于某种程度的过时状态了。但这就是一种很好的方式，可以将本地的东西安全地对外暴露。而且这样构建东西也很有趣。不知道别人怎么想，但我最近真的很享受边聊边构建的感觉。我记得几年前，Replit——那个专门让构建东西变得很简单的 AI 公司——当时我觉得：我为什么要在手机上构建东西？但现在我想我算是被"手机上瘾"了。就是这样，享受其中。我觉得这也是 OpenClaw 的精髓之一，用你自己的方式来使用它。我觉得我创建的 Clawspace 非常有用，你可以构建自己的工具。当然一定要考虑安全性，因为很多人在无意中暴露了一些东西，比如有人不小心删了所有的邮件之类的。不过，我发现可信代理关闭模式真的很有用，在那个 issue 里至少还有一个人也这么认为。我鼓励大家去试试，享受构建的过程，就这些了。我是 Nick Taylor，这就是我使用 OpenClaw 的方式。

>> 非常感谢你，Nick。我相信你现在可以松一口气，离开舞台去吃午饭了。

>> 好的。

>> 其实大家都可以去吃午饭了。现在是午餐时间。我们有一个不错的午餐休息时间，下午 2:30 回来。如果你们回到这个房间，我们还有，我想大概是三场分组会议，然后再有一次休息。休息之后回来，就是全体大会的主题演讲环节，所有分会场合并为一个大会场。另外提醒一下，如果你想发言或提交演讲申请，请加入 Slack。好了，感谢今天上午的各位演讲者。好好享用午餐，和大家多交流，下午 2:30 见。谢谢大家。
